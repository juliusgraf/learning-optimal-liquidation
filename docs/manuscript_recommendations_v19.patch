--- a/paper/main.tex
+++ b/paper/main.tex
@@ -393,23 +393,23 @@
 \paragraph{Reward} We define the reward of the shaped specification on three regions as explained below.
 
 \begin{enumerate}
-    \item During the continuous trading session for $t<\tau^{\op}$. The market maker submit a price $S_t^\bullet=S_t^\text{mid}+\alpha A_t^2$. The volume executed is given by $E_t$. The cash proceeds are thus given by $S_t^\bullet\times E_t$. We moreover assume that the market maker penalizes the execution by comparing the price executed with the hypothetical clearing price $H_t^{\cl} = \varphi(X_t)_3$. If $S_t^\bullet>H_t^{\cl}$, the market maker receives the full profit otherwise if $S_t^\bullet<H_t^{\cl}$ the market maker may regret the execution. We assume that the difference between $X^3$ and $S^\bullet$ tolerated is given by $k^\star\alpha$ for some $k^\star$ fixed. In the numerical specification, the CLOB reward multiplier is strictly positive when
+    \item During the continuous trading session for $t<\tau^{\op}$. The market maker submit a price $S_t^\bullet=S_t^\text{mid}+\alpha A_t^2$. The volume executed is given by $E_t$. The cash proceeds are thus given by $S_t^\bullet\times E_t$. We moreover assume that the market maker penalizes the execution by comparing the price executed with the hypothetical clearing price $H_t^{\cl} = \varphi(X_t)_3$. If $S_t^\bullet>H_t^{\cl}$, the market maker receives the full profit otherwise if $S_t^\bullet<H_t^{\cl}$ the market maker may regret the execution. The multiplier has clipping scale $k^\star\alpha$ for fixed $k^\star$. We interpret $S_0/(k^\star\alpha)$ as the local opportunity-cost preference, rather than treating the clipping scale as an empirically tolerated market-price gap. In the numerical specification, the CLOB reward multiplier is strictly positive when
 \[(H_t^{\cl}-S_t^\bullet)_+<k^\star\alpha,\]
 and vanishes when this gap is at least $k^\star\alpha$. It equals one when $S_t^\bullet\geqslant H_t^{\cl}$. We thus introduce a penalty function $f^c:\mathbb R\longrightarrow [0,1]$ continuous and increasing such that $f^c$ is zero on $\mathbb{R}_-$ and $f^c(k^\star\alpha)=1$, such that the reward of the market maker is given by 
 \[r_t(X_t,A_t)= S_t^\bullet E_t f^c(k^\star \alpha -( H_t^\cl -S_t^\bullet)_+).\]
 \item During the auction trading session for $\tau^{\op}\leqslant t<\tau^{\cl}$. The market maker submits a slope $K_t^a = A_t^3$ and a price
 \[S_t^a = S_{\tau^\op}^\mathrm{mid}+\alpha \left(\left\lfloor \frac{H_t^\cl-S_{\tau^\op}^\text{mid}}{\alpha}+\frac{1}{2}\right\rfloor+A_t^4\right).\]
-The agent receives a ``fictive'' reward $K_t^aH_t^\cl(H_t^\cl-S_t^a)$, where $H_t^\cl$ is the anticipated clearing price (were the auction to close at time $t$). The agent is penalized for canceling previous orders at cost $d_t$ at time $t$, yielding a penalty $-d_tc_t$. Here, $d_t$ is an exchange cancellation fee. Because the auction action set is two-sided, a submitted schedule may imply a projected purchase at the indicative price. This happens when $H_t^\cl \leqslant S_t^a$: the market maker is willing to buy $K_t^a(S_t^a-H_t^\cl)$ shares at price $H_t^\cl$ or below. We attenuate the negative cash-like component associated with projected purchases. This makes purchases less costly in the shaped objective and need not promote liquidation; the terminal inventory penalty remains a separate incentive to reduce terminal exposure. We hence introduce a function $f^a:\mathbb R\longrightarrow \mathbb R_+$ convex, continuous and decreasing such that $f^a$ is zero on $\mathbb{R}_+$ and $f^a(x) \leqslant (-x)_+$. The term $f^a(K_t^a H_t^\cl(H_t^\cl-S_t^a))$ attenuates or even removes the adverse consequence of buying. This attenuation is neither an admissibility constraint nor an economic cash flow. To simplify notation, introduce $u_t = K_t^a H_t^\cl (H_t^\cl-S_t^a) + f^a(K_t^a H_t^\cl(H_t^\cl-S_t^a))$ The reward of the market maker is given by
+Fix a dimensionless shaping weight $\omega_{\mathrm{sh}}\in[0,1]$. The agent receives a ``fictive'' reward $\omega_{\mathrm{sh}}K_t^aH_t^\cl(H_t^\cl-S_t^a)$, where $H_t^\cl$ is the anticipated clearing price (were the auction to close at time $t$). The agent is penalized for canceling previous orders at cost $d_t$ at time $t$, yielding a penalty $-d_tc_t$. Here, $d_t$ is a stylized liquidity-withdrawal cost; its numerical value is not an estimate of a universal exchange cancellation fee. Because the auction action set is two-sided, a submitted schedule may imply a projected purchase at the indicative price. This happens when $H_t^\cl \leqslant S_t^a$: the market maker is willing to buy $K_t^a(S_t^a-H_t^\cl)$ shares at price $H_t^\cl$ or below. We attenuate the negative cash-like component associated with projected purchases. This makes purchases less costly in the shaped objective and need not promote liquidation; the terminal inventory penalty remains a separate incentive to reduce terminal exposure. We hence introduce a function $f^a:\mathbb R\longrightarrow \mathbb R_+$ convex, continuous and decreasing such that $f^a$ is zero on $\mathbb{R}_+$ and $f^a(x) \leqslant (-x)_+$. The term $f^a(K_t^a H_t^\cl(H_t^\cl-S_t^a))$ attenuates or even removes the adverse consequence of buying. This attenuation is neither an admissibility constraint nor an economic cash flow. To simplify notation, introduce $u_t = \omega_{\mathrm{sh}}\left[K_t^a H_t^\cl (H_t^\cl-S_t^a) + f^a(K_t^a H_t^\cl(H_t^\cl-S_t^a))\right]$. The reward of the market maker is given by
 \[r_t(X_t,A_t) = u_t - c_t\left[d_t+ \sum_{s<t}\mathbf{1}_{\{K_s^a>0\}}\left(1-\theta_t^{(s-\tau^\op+1)}\right)u_s\right].\]
-The ``fictive'' reward during the auction is a training-shaping term designed to provide a denser learning signal for actions that improve the agent's position relative to the current indicative clearing price. When a live order is canceled, the exact fictive reward originally credited to that order is subtracted once as a clawback.
+The ``fictive'' reward during the auction is a training-shaping term designed to provide a denser learning signal for actions that improve the agent's position relative to the current indicative clearing price. When a live order is canceled, the exact weighted fictive reward originally credited to that order is subtracted once as a clawback. The weight does not multiply actual cash, inventory risk or cancellation costs. The original specification is recovered at $\omega_{\mathrm{sh}}=1$; the headline uses $\omega_{\mathrm{sh}}=\alpha/S_0=10^{-4}$. This changes the shaped objective, denoted $J_{\omega_{\mathrm{sh}}}$, and is not merely numerical reward rescaling.
 \item Final reward at the clearing for $t=\tau^{\cl}$. At the clearing time, the clearing price $S_{\tau^\cl}^\cl$ is determined and orders are matched. The market maker makes the profit or loss $S_{\tau^\cl}^\cl Z_{\tau^\cl}$ based on the orders she sent to the market and did not cancel by the clearing time. The agent is furthermore penalized for holding inventory. We introduce $\lambda > 0$ as a penalization parameter. Furthermore, the agent's reward associated with purchases is once again attenuated. Since the midprice is frozen throughout the auction phase in our model, any inventory remaining after the auction is priced at the last exogenous midprice $S^\text{mid}_{\tau^\op}$ rather than at the auction clearing price. This separates the valuation of residual inventory from the endogenous auction price, which may be affected by the agent's own submitted schedules. The final reward of the market maker is given by
 \begin{align*}
-    G(X_{\tau^\cl}) &= S_{\tau^\cl}^\cl Z_{\tau^\cl}- \lambda |I_{\tau^\cl}|^2 + f^a\left (S_{\tau^\cl}^\cl Z_{\tau^\cl}\right) + S^\text{mid}_{\tau^\op}I_{\tau^\cl}.
+    G(X_{\tau^\cl}) &= S_{\tau^\cl}^\cl Z_{\tau^\cl}- \lambda |I_{\tau^\cl}|^2 + \omega_{\mathrm{sh}} f^a\left (S_{\tau^\cl}^\cl Z_{\tau^\cl}\right) + S^\text{mid}_{\tau^\op}I_{\tau^\cl}.
 \end{align*}
 \end{enumerate}
 
 \begin{remark}
-    In the numerical part we will choose $f^c (t) = \min(1,\frac{t_+}{k^\star \alpha})$ and $f^a(t) = q(-t)_+$ for some $q\in[0,1]$. One can interpret the penalty as removing a fraction $q$ of the reward. With $q = 1$, the purchase-side component is neutralized.
+    In the numerical part we will choose $f^c (t) = \min(1,\frac{t_+}{k^\star \alpha})$ and $f^a(t) = q(-t)_+$ for some $q\in[0,1]$. The function adds back fraction $q$ of a negative cash-like component before applying $\omega_{\mathrm{sh}}$. At $q=1$ that fictive purchase component is neutralized; the headline preference $q=0$ gives no purchase subsidy. Actual purchase cash is always paid in full in economic evaluation.
 \end{remark}
 
 To summarize, at time $t<\tau^\cl$ the random one-step reward is
@@ -427,7 +427,7 @@
 
 The learning objective of the agent is to solve
 \begin{align*}
-    (\mathbf{P})\quad \sup_{\pi \in \Pi} \quad & J(\pi) = \mathbb{E}_\pi\left [\sum_{i=0}^{m} r_{t_i}(X_{t_i},A_{t_i}) + G(X_{\tau^\cl}) \right] \nonumber \\
+    (\mathbf{P})\quad \sup_{\pi \in \Pi} \quad & J_{\omega_{\mathrm{sh}}}(\pi) = \mathbb{E}_\pi\left [\sum_{i=0}^{m} r_{t_i}(X_{t_i},A_{t_i}) + G(X_{\tau^\cl}) \right] \nonumber \\
     \text{s.t.} \quad & \left \{\begin{array}{l}
     X_0 \sim \mu_0  \\
     A_{t_i} \sim \pi(\cdot \mid \varphi(X_{t_i}))
@@ -447,15 +447,192 @@
 \end{array}\right..
 \end{align*}
 
-Without reward shaping or initial-inventory centering, $J(\pi)=\bar J_\lambda(\pi)+S_0^{\mathrm{mid}}I_0$. The unshaped training criterion and the reported risk-adjusted economic criterion differ only by a constant, which unifies the base training objective with the economic objective. For precision, by the unshaped specification, we mean that the CLOB multiplier is set identically equal to one, the entire interim auction term $K_t^aH_t^\cl(H_t^\cl-S_t^a) + f^a\left(K_t^aH_t^\cl(H_t^\cl-S_t^a)\right)$ is omitted, and the terminal $f^a$ term is set to zero.\vspace{0.5em}
+Without reward shaping or initial-inventory centering, $J(\pi)=\bar J_\lambda(\pi)+S_0^{\mathrm{mid}}I_0$. The unshaped training criterion and the reported risk-adjusted economic criterion differ only by a constant. Headline runs train on the weighted shaped criterion $J_{\omega_{\mathrm{sh}}}$; the unshaped criterion defines a treatment. Validation, checkpoint selection and all final policy comparisons use $\bar J_\lambda$. For precision, by the unshaped specification, we mean that the CLOB multiplier is set identically equal to one, the entire interim auction term $K_t^aH_t^\cl(H_t^\cl-S_t^a) + f^a\left(K_t^aH_t^\cl(H_t^\cl-S_t^a)\right)$ is omitted, and the terminal $f^a$ term is set to zero.\vspace{0.5em}
+
+The shared stylized calibration uses $S_0=100$, $\alpha=.01$, $I_0=100$,
+$\lambda=.01$, $q=0$, $k^\star=10000$, $\beta=.25$ and $d=.001$.
+The parameter $k^\star$ is a dimensionless number of ticks: its clipping
+distance is indeed 10000 ticks, or $k^\star\alpha=100$ model-price units.
+This is not a realistic tolerated market-price gap. To interpret the training
+preference, define $\eta_{\mathrm C}=S_0/(k^\star\alpha)=1$ and
+$g=(H-p)_+$. The exact per-execution CLOB reward is
+\[
+ r^{\mathrm C}(p,E,H)=pE-\eta_{\mathrm C}\frac{p}{S_0}E
+ \min\{g,S_0/\eta_{\mathrm C}\}.
+\]
+Thus, locally at $p=S_0$, a one-tick shortfall deducts one tick per unit sold.
+Reducing $k^\star$ to 100 would make the same local deduction 100 times
+stronger. The economically interpretable preference is $\eta_{\mathrm C}$;
+$k^\star$ retains compatibility with the manuscript's gross-proceeds formula.
+Neither quantity is estimated from observed exchange price tolerances.
+$q=0$ gives no purchase subsidy. The auction weight makes the fictive credit
+approximately one tick per projected unit, reducing the spurious incentive
+to sell beyond liquidation; it does not make shaped and economic objectives
+equivalent. A 10-unit residual costs $\lambda10^2=1$, equal to one tick on
+the initial inventory. A maximum schedule has slope $32\beta=8$ and offers
+.8 units at a ten-tick local gap; 30 such schedules offer 24 nominal units.
+These are scale diagnostics, not hard execution bounds. Actual signed
+pro-rata fills remain unrestricted by terminal inventory.
+The modeled two-tick spread is 2 basis points at $S_0$; historical prices are
+rebased, so this is not necessarily a one-cent tick in raw share prices.
+In the training split, median daily selected-quote spreads are 5.6275 bps
+(CAT), .8679 (GOOGL), 2.0769 (JPM), 1.4095 (MSFT), and 1.3842 (PG).
+The frozen sidecar mislabeled quote sizes as round lots: Alpaca's CTA/UTP
+fields have been in shares since November 3, 2025.\footnote{\url{https://docs.alpaca.markets/us/v1.1/changelog/marketdata-bid-and-ask-size-display-change}}
+The pooled training median of daily selected best-quote sizes is 80 shares.
+The model's $2\operatorname{Beta}(2,5)$ top-depth median is .5289 inventory
+units. Approximately 150 shares per model inventory unit is therefore an
+illustrative top-depth normalization. It is not a fit of full depth or trade
+flow; intensities represent stylized eligible flow, not the complete SIP tape.
+Results are reported in normalized units or basis points of initial notional.
+For a dimensional dollar interpretation let $M$ be shares per inventory unit
+and $A=P_0^{\mathrm{raw}}/S_0$ be dollars per model-price unit. Dollar PnL is
+$MA$ times model PnL, physical slope quantum is $M\beta/A$ shares per dollar,
+and the physical quadratic coefficient is $A\lambda/M$ dollars per share
+squared. The rebased model tick is $A\alpha$ dollars, not necessarily one
+raw cent. These conversions do not establish empirically identified auction
+depth or transaction costs.
+Quote data cannot identify auction slopes or market-order intensities.
+The exogenous slope support $[1,20]$ and order-flow
+parameters are stylized; historical experiments replay midprices within the
+same simulated market. The reference policies retain their stated formulas.
 
 \section{Learning Algorithms}
 \label{sec:learning}
 
-In this section, we explain the numerical methods used to approximately solve $(\bar{\mathbf{P}}_\lambda)$ and $(\mathbf{P})$ in the shaped specification.
+In this section, we explain the numerical methods used to approximately solve $(\mathbf{P})$ in the headline shaped specification and $(\bar{\mathbf{P}}_\lambda)$ in the shaping-off treatment.
+
+\paragraph{Forecast reliability.}
+Let $H_t^{\mathrm{raw}}$ denote the output of Algorithm
+\ref{alg:hyp_clearing_price}. During the CLOB we use the future-close forecast
+\[
+H_t^{\cl}=S_t^{\mathrm{mid}}+
+ w_{b(t)}(H_t^{\mathrm{raw}}-S_t^{\mathrm{mid}}),\qquad
+ b(t)=\min\{3,\lfloor4t/\tau^{\mathrm{op}}\rfloor\}.
+\]
+For each of the four equal time bins, define $x_i=H_i^{\mathrm{raw}}-S_i^{\mathrm{mid}}$
+and $y_i=S_i^{\cl}-S_i^{\mathrm{mid}}$ on 256 independent no-order training paths,
+and fit $w_b=\operatorname{clip}_{[0,1]}(\sum_i x_i y_i/\sum_i x_i^2)$.
+A zero denominator gives zero weight. The fit has no intercept and uses no
+validation or test outcomes. The weights are frozen across all policies and
+mechanism cells. They are $(0,.190153,.234565,.414256)$ in the synthetic
+setting and $(.192784,.314044,.386180,.525631)$ from the pooled historical
+training sessions. These are predictive reliability coefficients, not
+exchange microstructure parameters. The raw Algorithm 1 recursion and its
+exogenous carryover are unchanged. During the auction we retain the original
+indicative calculation without this shrinkage. Forecast errors are reported
+separately by market phase and horizon; pooled error need not describe the
+signal's usefulness earlier in the CLOB. A more accurate forecast need not
+improve the economic value of a policy trained on the shaped criterion.
+
+\paragraph{Numerical conditioning and replay.}
+All learners use the same information in the 18-coordinate observation.
+Before fitting normalization, replace $H$ by $H-S^{\mathrm{mid}}$ and the
+slope-weighted quotes $W_a,W_e$ by $W_a-S^{\mathrm{mid}}G_a$ and
+$W_e-S^{\mathrm{mid}}G_e$. In the auction define
+\[
+\Delta=\frac{W_a-S^{\mathrm{mid}}G_a+W_e-S^{\mathrm{mid}}G_e+D}{G_a+G_e},
+\qquad \widehat Z=G_a\Delta-(W_a-S^{\mathrm{mid}}G_a),
+\]
+where $D$ is observed buy-minus-sell taker volume. Replace the two quote-moment
+coordinates by $I-\widehat Z$ and $\Delta$, retaining the slopes, inventory
+and imbalance. This is invertible when $G_a+G_e>0$, uses no future clearing
+information, and does not require the ablated $H$ signal. CLOB-inactive
+raw moments remain zero. Fit population means and standard deviations on
+16 dedicated training trajectories covering persistence, cancellations,
+abstention and large opening inventory, then freeze and checkpoint them.
+Time and decision index divide by $\tau^{\mathrm{cl}}$; the cancellation
+indicator is unchanged. Validation and test never fit the transform.
+
+DQN, DDPG and TD3 additionally transform auction inventory $I$ and continuous-root
+residual exposure $I-\widehat Z$ by $x\mapsto\operatorname{asinh}(x/I_s)$,
+where $I_s=\alpha/\lambda$ (use $I_0$ if $\lambda=0$). The scale $I_s$ is
+the inventory at which one-tick execution value $\alpha I_s$ equals the
+quadratic penalty $\lambda I_s^2$. The transform is signed, unbounded and
+invertible; it neither caps fills nor supplies additional information.
+These three algorithms fit separate CLOB and auction population statistics
+on the same dedicated training paths. SAC retains pooled linear
+standardization. Consequently the headline compares the specified algorithm
+implementations, including their preprocessing, rather than isolating an
+algorithm-only causal effect. Supplementary matched representation controls
+apply pooled linear preprocessing to all methods or apply the phase-specific
+signed-asinh preprocessing to all methods. Rewards, controls and optimizer
+settings are unchanged by those representation controls.
+
+
+Initial-inventory centering subtracts the constant $S_0I_0$ over an episode.
+Let $R_i$ be the resulting immediate reward including the terminal component
+on the final transition. Replay uses
+\[
+\widetilde R_i=R_i+\Phi(y_{i+1})-\Phi(y_i)
+-\mathbf1_{\{t_i<\tau^{\mathrm{op}}\}}q_{\mathrm{ref}}((t_i+t_{i+1})/2)
+ (S^{\mathrm{mid}}_{t_{i+1}}-S^{\mathrm{mid}}_{t_i}).
+\]
+Set $\Phi(\mathrm{terminal})=0$. During CLOB trading,
+\[
+\Phi(y)=(S^{\mathrm{mid}}-S_0)I
+-\lambda\max\{I-I_0(1-t/\tau^{\mathrm{op}}),0\}^2.
+\]
+In the auction, set $\Phi(y)=(S^{\mathrm{mid}}-S_0)I+
+\Delta\widehat Z-\lambda(I-\widehat Z)^2$.
+The potential telescopes and has initial value zero. In the economic-sparse
+auction control, omit $\Delta\widehat Z-\lambda(I-\widehat Z)^2$ during the
+auction while retaining the mark-to-mid term, CLOB potential and terminal
+correction. The midprice is frozen during this phase, so persistent unchanged
+orders receive no action-dependent intermediate potential credit. Dense and
+sparse auction conditioning therefore share the same episode objective.
+The deterministic
+reference curve $q_{\mathrm{ref}}$ is the mean CLOB inventory path from the
+same training calibration, excluding the deliberately no-CLOB mode;
+interpolate onto minute times and freeze it before learning. Subtracting its
+realized price return removes the same amount from every policy on a common
+exogenous price/clock path, preserving policy differences. Its historical
+expectation need not be zero. Neither adjustment enters reported shaped J,
+PnL or $\bar J_\lambda$. Actual settlement remains tick-price pro-rata.
+
+All methods use one-step replay. CLOB junction rows continue through the
+auction target network; only the final transition is terminal. The first
+32 training episodes share a finite exploration design: four CLOB modes
+(maximum volume at offset one, abstention, and two random fixed proposals)
+cross four auction modes (no order, persistent sells, persistent buys,
+and cancel/replace sells), twice. Random slopes and offsets are sampled
+independently of future data; no benchmark actions or optimization labels
+are supplied. Both phase learners begin updating after the 32 complete warm-up episodes
+and at least 512 transitions have been stored in their respective buffers. Subsequent exploration follows each method's stated
+rule. Initial learning rates share the fixed factor
+$\max\{.1,2^{-e/90}\}$, independent of outcomes. Evaluation is greedy.
+
+DDPG additionally applies LayerNorm with learned affine parameters after
+each hidden critic linear layer and before its ReLU activation. Both hidden
+layers have width 256; the final scalar value remains unrestricted. The
+Stable-Baselines3 policy factory constructs both online and target critics
+with this architecture. The actor, single-critic update, actor update at every
+step, absence of target smoothing, learning rates and Polyak coefficient are
+unchanged. TD3 and SAC retain their specified critic architectures. Thus this
+is critic conditioning within native DDPG, without substituting TD3 updates.
+
+Production checkpoint selection maximizes mean $\bar J_\lambda$ on 128
+fixed validation paths, evaluated every 50 training episodes, with an
+800-episode cap. A checkpoint becomes eligible after 2000 gradient updates
+in each active phase. The first eligible checkpoint is evaluated immediately,
+even between periodic validation dates. Training stops after four eligible
+evaluations without improvement. Every training seed is retained regardless
+of improvement over initialization; the untrained policy remains a diagnostic.
+Neither reference-policy performance nor test outcomes enter selection.
 
 \subsection{Deep Q-Network}
 \label{subsec:dqn}
+
+The v18 Q approximator applies non-affine layer normalization before each
+hidden activation in the state encoder. Its learned action embedding is
+layer-normalized and passed through a hyperbolic tangent. This bounds the
+embedding while retaining unrestricted linear query and value heads; neither
+rewards nor Q targets are clipped. AdamW uses weight decay $10^{-4}$.
+This addresses amplification between the two learned factors in the bilinear
+Q representation. Replay uses the one-step Double-DQN target below; a
+development trial with uncorrected multi-step returns was rejected because
+it included subsequent exploratory actions in an earlier action's target.
+
 
 We remind that $\mathcal Y=\varphi(\mathcal X)$ is the feature space used by the
 learning algorithms. We consider the online episodic RL setting. In this setting, the agent executes the process sequentially for $E$ episodes and we denote the total number of episode samples $N_s = \sum_{e=0}^{E-1}(m_e+1)$. We index training episodes by $e=0,\ldots,E-1$. The transition law and conditional expected rewards of the simulator are not assumed known. We will therefore use a model-free method to train an approximately optimized feature-based policy $\hat\pi$ maximizing $J$.
@@ -463,17 +640,37 @@
 %\[\forall (y,a)\in \mathcal{Y}\times \mathcal{A},\quad Q_{\theta}(y,a) = Q_\phi(y,a) \mathbf{1}_{\{t(y)<\tau^\op\}}+Q_\psi(y,a) \mathbf{1}_{\{t(y)\geqslant \tau^\op\}}\] where $\theta = (\phi,\psi)$ and $\phi\in \mathbb{R}^{q_1},\psi \in \mathbb{R}^{q_2}$ and $q_1 + q_2 = q$.
 We approximate the action-value function by a neural network $Q_\theta(y,a)$ for some weight $\theta \in \mathbb{R}^q$, where $q \geqslant  1$. For each phase $\kappa\in\{c,a\}$, we use a separate coordinate-conditioned action-value network $Q_{\theta_\kappa}$, for which we quickly want to specify the considered architecture. Let $z_\kappa(a)$ denote the normalized coordinates of action $a$. A phase-specific state trunk, consisting of two ReLU hidden layers, maps the feature vector $y$ to a scalar $V_{\theta_\kappa}(y)$ (that is the state-only component of the network) and a query vector $u_{\theta_\kappa}(y)\in\mathbb{R}^{r}$ for $r\geqslant 1$. We then use a phase-specific action encoder, whose parameters are shared across all actions in that phase, to map $z_\kappa(a)$ to
 $e_{\theta_\kappa}(a)\in\mathbb{R}^{r}$. We finally consider
-\[ Q_{\theta_\kappa}(y,a) = V_{\theta_\kappa}(y) + \frac{\left\langle u_{\theta_\kappa}(y),e_{\theta_\kappa}(a)\right\rangle}{\sqrt{r}} + \omega_\kappa \mathbf{1}_{\{a\neq 0\}},\]
-where $\omega_\kappa$ is a learned scalar shared by all nonzero actions. This parameterization shares statistical information across actions having similar coordinates instead of assigning an unrelated output vector to every grid point, as it is the case for vanilla DQN architectures. For $y\in \mathcal{Y}$, let $t(y)$ be the time coordinate of $y$. To summarize, we define a separate neural network for each phase:
+\[ Q_{\theta_\kappa}(y,a) = V_{\theta_\kappa}(y) + \frac{\left\langle u_{\theta_\kappa}(y),\widetilde e_{\theta_\kappa}(a)\right\rangle}{\sqrt{r}} + \omega_\kappa \mathbf{1}_{\{a\neq 0\}},\]
+where $\widetilde e_{\theta_c}(a)=e_{\theta_c}(a)$ and
+$\widetilde e_{\theta_a}(a)=e_{\theta_a}(a)-e_{\theta_a}(0)$.
+The CLOB coefficient $\omega_c$ is learned, whereas $\omega_a=0$ is fixed.
+Thus $Q_{\theta_a}(y,0)=V_{\theta_a}(y)$: the auction reference value is learned
+directly, and the action encoder learns differences from that reference.
+The greedy policy still maximizes the learned Q-function over every admissible
+action; no benchmark policy or economic-value override is used. This parameterization shares statistical information across actions having similar coordinates instead of assigning an unrelated output vector to every grid point, as it is the case for vanilla DQN architectures. For $y\in \mathcal{Y}$, let $t(y)$ be the time coordinate of $y$. To summarize, we define a separate neural network for each phase:
 \[\forall (y,a)\in \mathcal{Y}\times \mathcal{A},\quad Q_\theta(y,a) = Q_{\theta_c}(y,a)\mathbf{1}_{\{t(y)<\tau^\op\}} + Q_{\theta_a}(y,a)\mathbf{1}_{\{t(y)\geq\tau^\op\}}.\]
 
 The terminal Q-function is given by the reward $G(X_{\tau^\cl})$ and we do not need to define a neural network. We train by Q-learning with experience replay and target networks (DQN, \cite{mnih2015human}) using the Double-DQN target of \cite{van2016deep}, and ensuring junction at $t=\tau^\op$ when the phase switch occurs. Note that the $\varepsilon$-greedy training policy is stochastic, while the reported greedy evaluation policy is deterministic after applying solving ties by the first action in lexicographic grid order. For DQN, we use the same masked exploration schedule $(\varepsilon_e)_{e\geqslant 0}$ in both phases, given by
 \[\varepsilon_e =\left\{\begin{array}{ll}
-    1 & \text{if } 0\leqslant e<50 \\
-    1-0.99(e-50)/600 & \text{if } 50 \leqslant e \leqslant 650 \\
-    0.01 & \text{if } e>650
+    1 & \text{if } 0\leqslant e<5 \\
+    1-0.95(e-5)/90 & \text{if } 5 \leqslant e \leqslant 95 \\
+    0.05 & \text{if } e>95
 \end{array} \right..\]
-Conditional on exploration, an action is drawn uniformly from $\operatorname{Adm}(y)$. In the final evaluation we use $\varepsilon=0$. For the auction network, the value and query heads are initialized to zero and $\omega_a=-0.02$ in replay reward units, so the canonical no-order action initially has value zero and every other action has value $-0.02$. The scalar $\omega_a$ and all network weights remain trainable; this is an initialization prior rather than an admissibility constraint.
+After the common first 32 exploration episodes, conditional on epsilon
+exploration, CLOB actions are uniform over $\operatorname{Adm}(y)$. In the
+auction, define $\mathcal C(y)=\{a\in\operatorname{Adm}(y):K^a=0\}$, containing
+waiting and, when admissible, cancellation without a new order. The exploration
+distribution is
+\[
+\nu_c(y)=\mathcal U(\operatorname{Adm}(y)),\qquad
+\nu_a(y)=\tfrac12\mathcal U(\mathcal C(y))+
+         \tfrac12\mathcal U(\operatorname{Adm}(y)).
+\]
+This gives the two distinct zero-slope controls sufficient coverage without
+removing any admissible action. In final evaluation $\varepsilon=0$.
+Both phase networks use ordinary PyTorch linear-layer initialization, with
+$\omega_c=0$ initially and $\omega_a=0$ fixed as above. No no-order margin
+is imposed at initialization.
 
 \begin{algorithm}[ht!]
 \caption{Double DQN with phase-specific networks}
@@ -498,25 +695,27 @@
         \STATE Set $y_i\gets\varphi(X_{t_i})$ and $\kappa_i \gets c \mathbf{1}_{\{t_i < \tau^\op\}} + a \mathbf{1}_{\{t_i \geqslant \tau^\op\}}$
         \STATE Select
         \[a_i \sim \left\{\begin{array}{ll}
-        \mathcal U(\operatorname{Adm}(y_i)),
+        \nu_{\kappa_i}(y_i),
         &\text{with probability }\varepsilon_e,\\ \arg\max_{a\in\operatorname{Adm}(y_i)}
         Q_{\theta_{\kappa_i}}(y_i,a),
         &\text{with probability }1-\varepsilon_e
         \end{array}\right.
         \]
 
+        \STATE If $e<32$, replace $a_i$ by the common finite exploration proposal.
         \STATE Apply $a_i$ at $t_i$ and observe the one-step reward
         $r_i$ and the next simulator state $X_{t_{i+1}}$
 
         \STATE Set $\eta_i\gets\mathbf1_{\{i=m_e\}}$ and $g_i\gets
-        \eta_i
-        G(X_{\tau^\cl})$
+        \eta_i\bigl[G(X_{\tau^\cl})-S_0 I_{\tau^{\mathrm{op}}}\bigr]$
         \STATE If $\eta_i=0$, set
         $y_{i+1}\gets\varphi(X_{t_{i+1}})$;
         otherwise set $y_{i+1}\gets\varnothing$
 
-        \STATE Store $(y_i,a_i,c_rr_i,y_{i+1},
-        \eta_i,c_rg_i)$ in $\mathcal D_{\kappa_i}$
+        \STATE Form the centered and conditioned total reward $\widetilde R_i$
+        defined above. Store $(y_i,a_i,c_r(\widetilde R_i-g_i),y_{i+1},
+        \eta_i,c_rg_i)$ in $\mathcal D_{\kappa_i}$.
+        The terminal target below therefore contains $G$ exactly once.
 
         \IF{$|\mathcal D_{\kappa_i}|\geqslant  N_w^{\kappa_i}$}
             \STATE Sample $\mathcal B=
@@ -545,7 +744,7 @@
             \ell\left(
             Q_{\theta_{\kappa_i}}(y_j,a_j)-Y_j
             \right)$
-            where $\ell$ is the Huber loss
+            where $\ell(x)=x^2$
 
             \STATE Update $\theta_{\kappa_i}^-
             \gets
@@ -569,7 +768,7 @@
 $v=\min\{\lfloor I_t\rfloor,V_{\max},\lfloor (u_1+1)V_{\max}/2+1/2\rfloor\}$ and $\delta=\lfloor (u_2+1)L_{\max}/2+1/2\rfloor$, with $\delta=0$ when $v=0$. In the auction, $K^a=\beta\lfloor (u_1+1)K_{\max}/2+1/2\rfloor$, $\ell=\lfloor B_{\max}u_2+1/2\rfloor$ and $c=\mathbf1_{\{u_3\geqslant0\}}C(X_t)$.
 The local offset is clipped to its state-dependent admissible interval; if that interval is empty the slope is set to zero. Zero slope uses the canonical offset zero. \vspace{0.5em}
 
-We consider three off-policy actor-critic algorithms: Deep Deterministic Policy Gradient (DDPG), Twin Delayed DDPG (TD3), and Soft Actor-Critic (SAC), see respectively \cite{lillicrap2020continuous}, \cite{fujimoto2018addressing}, and \cite{haarnoja2018soft}. DDPG uses a deterministic actor with exploration noise. TD3 adds twin critics, target-policy smoothing, and delayed actor updates. Finally, SAC uses a stochastic actor and entropy regularization. These methods are natural continuous-control counterparts to Q-learning. and they all learn both the critic $Q_\theta$. DDPG and TD3 learn deterministic phase-specific actors $\mu_\omega \colon y\in \mathcal{Y}\longmapsto \mu_\omega(y)\in \mathcal U_{\kappa(y)}$ with weights $\omega \in \mathbb{R}^l$ with $l\geqslant 1$, such that $\Gamma_y(\mu_\omega(y)) \in \mathrm{Adm}(y)$. SAC instead learns a stochastic policy $\pi_\omega(\cdot\mid y) \in \mathcal{P}(\mathcal U_{\kappa(y)})$, and not a point map. The main difference with DQN is that the maximization over actions in the Bellman target is no longer computed by enumerating all admissible actions. Instead, the next action is produced by a learned policy. We refer to \cite{achiam2018spinningup} for details on each of DDPG, TD3 and SAC and summarized in the following parameter table used for all numerical simulations. Each actor–critic method uses separate phase-specific actors, critics, target networks and replay buffers, and uses the auction critic for the continuation target at the phase boundary. The deterministic auction actors and the mean of the SAC auction policy are initialized at the normalized proposal $(-1+0.25/K_{\max},0,-0.5)$, which projects to the canonical no-order action. SAC's initial pre-squash log standard deviation is $-2.5$ in each auction coordinate. SAC is evaluated using its deterministic mean proposal, while its training policy remains stochastic.
+We consider three off-policy actor-critic algorithms: Deep Deterministic Policy Gradient (DDPG), Twin Delayed DDPG (TD3), and Soft Actor-Critic (SAC), see respectively \cite{lillicrap2020continuous}, \cite{fujimoto2018addressing}, and \cite{haarnoja2018soft}. DDPG uses a deterministic actor with exploration noise. TD3 adds twin critics, target-policy smoothing, and delayed actor updates. Finally, SAC uses a stochastic actor and entropy regularization. These methods are natural continuous-control counterparts to Q-learning. and they all learn both the critic $Q_\theta$. DDPG and TD3 learn deterministic phase-specific actors $\mu_\omega \colon y\in \mathcal{Y}\longmapsto \mu_\omega(y)\in \mathcal U_{\kappa(y)}$ with weights $\omega \in \mathbb{R}^l$ with $l\geqslant 1$, such that $\Gamma_y(\mu_\omega(y)) \in \mathrm{Adm}(y)$. SAC instead learns a stochastic policy $\pi_\omega(\cdot\mid y) \in \mathcal{P}(\mathcal U_{\kappa(y)})$, and not a point map. The main difference with DQN is that the maximization over actions in the Bellman target is no longer computed by enumerating all admissible actions. Instead, the next action is produced by a learned policy. We refer to \cite{achiam2018spinningup} for details on each of DDPG, TD3 and SAC and summarized in the following parameter table used for all numerical simulations. Each actor–critic method uses separate phase-specific actors, critics, target networks and replay buffers, and uses the auction critic for the continuation target at the phase boundary. DDPG, TD3 and SAC use Stable-Baselines3 version 2.7.1 with its default network initialization, without a special auction no-order prior. The common finite exploration design overrides behavior in the first 32 episodes. If a phase buffer is still below its warm-up threshold afterward, its proposals are uniform in the normalized proposal box. A replay adapter supplies the auction continuation value for CLOB boundary transitions; losses, optimizer steps and target updates use the library implementations. Actor and critic optimizer hooks apply the common gradient-norm bound of 1 immediately before each library step. SAC uses automatic entropy adjustment initialized at $\alpha_{\mathrm{ent}}=10^{-3}$ in replay reward units. SAC is evaluated using its deterministic mean proposal, while its training policy remains stochastic.
 
   \begin{table}[ht!]
   \centering
@@ -581,13 +780,14 @@
   \midrule
   \multicolumn{5}{l}{\emph{Shared}}\\
   Replay capacity $M$             & \multicolumn{4}{c}{$50{,}000$} \\
-  Warm-up sizes $N_w^c$/$N_w^a$       & \multicolumn{4}{c}{$2{,}000/512$ (CLOB/Auction)} \\
+  Warm-up sizes $N_w^c$/$N_w^a$       & \multicolumn{4}{c}{$512/512$ (CLOB/Auction), after 32 episodes} \\
   Minibatch size $B$                   & \multicolumn{4}{c}{$128$} \\
-  Optimizer                            & \multicolumn{4}{c}{Adam} \\
-  Hidden layers (ReLU)                 & \multicolumn{4}{c}{$2\times 128$} \\
-  Gradient-norm clip                   & \multicolumn{4}{c}{$1.0$} \\
-  Reward scale $c_r$                   & \multicolumn{4}{c}{$10^{-3}$} \\
+  Optimizer                            & AdamW & Adam & Adam & Adam \\
+  Hidden layers (ReLU)                 & \multicolumn{4}{c}{$2\times 256$} \\
+  Gradient-norm clip                   & $10.0$ & $1.0$ & $1.0$ & $1.0$ \\
+  Reward scale $c_r$                   & \multicolumn{4}{c}{$1$} \\
   Gradient steps per env.\ step        & \multicolumn{4}{c}{$1$} \\
+  Replay horizon & $1$ & $1$ & $1$ & $1$ \\
   DQN action-embedding rank $r$ & $32$ & -- & -- & -- \\
   \midrule
   \multicolumn{5}{l}{\emph{Learning rates}}\\
@@ -596,14 +796,14 @@
   Temperature      & -                    & -                  & -                  & $3\times10^{-4}$ \\
   \midrule
   \multicolumn{5}{l}{\emph{Critics and target networks}}\\
-  Critic loss                          & Huber    & MSE      & MSE      & MSE \\
+  Critic loss                          & MSE    & MSE      & MSE      & MSE \\
   Number of critics                    & $1$      & $1$      & $2$      & $2$ \\
-  Polyak coefficient $\tau_{\mathrm{targ}}$ & $0.0025$ & $0.0025$ & $0.005$ & $0.005$ \\
+  Polyak coefficient $\tau_{\mathrm{targ}}$ & $0.005$ & $0.0025$ & $0.005$ & $0.005$ \\
   \midrule
   \multicolumn{5}{l}{\emph{Exploration}}\\
   Behaviour policy                     & $\varepsilon$-greedy & Gaussian & Gaussian & Stochastic \\
-  Range & $1 \to 0.01$ & - & - & - \\
-  Warm-up/decay-end (episode) & $50 / 650$  & - & - & - \\
+  Range & $1 \to 0.05$ & - & - & - \\
+  Warm-up/decay-end (episode) & $5 / 95$  & - & - & - \\
   Action-noise std & -     & $0.1$ & $0.1$ & - \\
   Target entropy    & -      & -  & -  & $-2$/$-3$ \\
   \midrule
@@ -666,6 +866,57 @@
 
 \section{Numerical Simulations}
 \label{sec:numerics}
+
+The v19 campaign fixes ten master seeds
+$\{7,42,99,123,314,577,811,1618,2024,2718\}$ and retains all 440
+algorithm--market--treatment runs. Each selected policy is evaluated on 100
+test episodes with matched environment seeds across policies and treatments.
+Uncertainty intervals resample training-seed means; test episodes are not
+independent training replications. The headline retains the calibrated $H$
+observation, both manuscript reward preferences, the indicative auction
+anchor and dense potential conditioning. Five additional synthetic cells
+separate the following mechanisms. A fixed-mid-anchor full-$J$ cell isolates
+anchoring from information. Economic dense and economic sparse cells retain
+$H$ and that fixed anchor, disable both manuscript preferences, and differ
+only in action-dependent auction potential credit. An economic dense $H$-off
+cell isolates the forecast observation. The fixed-anchor full-$J$ cell
+measures the combined preference effect. CLOB-only and auction-only shaping
+are optional follow-ups outside this final matrix; separate contributions
+cannot be inferred from the combined contrast. Finally,
+auction access compares the $H$-off economic cell with a no-auction cell.
+All comparisons retain the same market parameters and matched evaluation
+paths. The original bundled $H$/anchor and no-cancellation controls are not
+part of this canonical matrix.
+
+We report five paired economic effects, as well as raw dense-minus-sparse
+validation curves at common observed training budgets. We never forward-fill
+stopped runs or replace observed learning curves with a running maximum.
+This distinguishes the credit-assignment value of terminal-corrected guidance
+from the economic effect of extra shaped preferences. Either effect may be
+negative with function approximation and finite training. A separate
+same-CLOB no-order counterfactual decomposes direct auction execution value
+into price edge net of fees and terminal-inventory risk relief; it is not a
+retrained no-auction optimum.
+
+The continuous-market intensity is $\lambda_0=.5$ arrivals per minute per
+side. The capped Pareto mean is approximately $3.3104$ inventory units,
+giving expected contra-side volume $198.62$ over 120 minutes for a
+100-unit parent. This is a stylized substantial-participation liquidation
+scenario, not a fit of total exchange tape volume. Quarter-intensity and
+unit-intensity configurations are liquidity sensitivities; the auction's
+value depends on the inventory arriving at its opening.
+
+Historical training samples whole rebased sessions from all five stocks
+on August 3--14, 2026 (50 stock--session paths). Normalization uses only this
+training pool. Validation and evaluation remain specific to the requested
+stock on their disjoint date partitions. V19 uses a fresh simulation seed
+namespace (19001); the August 24--28 historical test dates were previously
+examined in v17 and v18, so this is not a new untouched historical holdout. The
+experiment measures generalization across the stated dates and simulated
+order flow, not profitability on unseen securities or an empirically fitted
+closing auction. Broader empirical claims require an additional frozen date
+block that was not used during model development.
+
 
 This section employs the generative stochastic market model formulated in Section \ref{sec:marketmodel} to simulate continuous trading and closing auctions. We compare the DQN-learned policy against the learned continuous-control methods and the two stylized reference benchmarks: Avellaneda-Stoikov (AS) strategy, see Section \ref{section:AS} and the TWAP strategy see Section \ref{section:TWAP}.  The goal of this section is to compare the DQN policy with the projected continuous-control policies and the stylized reference benchmarks under the same simulator configuration. We start by generating an emulator of the CLOB followed by the closing auction. The algorithm used to generate the market mechanism is defined in Section \ref{sub:generative} with Algorithm \ref{alg:generative_model}. In Section \ref{sub:rough} we generate mid price process values by using the rough-Heston to describe the evolution of the price of a risky asset, see \cite{gatheral2022volatility}.\vspace{0.5em}
 
--- a/paper/results/tables_params/params_generative.tex
+++ b/paper/results/tables_params/params_generative.tex
@@ -8,7 +8,7 @@
 $\tau^{\mathrm{op}}$ & 120 min & Auction opening time \\
 $\tau^{\mathrm{cl}}$ & 150 min & Clearing time \\
 $I_0$ & 100 & Initial inventory \\
-$\lambda_0$ & 1/min/side & Continuous phase Poisson intensity \\
+$\lambda_0$ & 0.5/min/side & Stylized continuous-phase arrival intensity \\
 $v_m$ & 2 & Pareto distribution scale parameter \\
 $\gamma_m$ & 2.5 & Pareto distribution shape parameter \\
 $V_\infty$ & 2 & Beta distribution scaling parameter \\
@@ -22,17 +22,18 @@
 $B_\infty$ & 150 & Common absolute strategic/exogenous auction price-deviation bound \\
 $B_\text{max}$ & 10 & Local auction policy-coordinate half-width \\
 $D_\mu$ & 0.1 & Numerical lower slope bound \\
-$U_1$ & 0.1 & Exogenous supply slope lower bound \\
-$U_2$ & 2 & Exogenous supply slope upper bound \\
+$U_1$ & 1 & Exogenous supply slope lower bound \\
+$U_2$ & 20 & Exogenous supply slope upper bound \\
 $p_1$ & 1 & One market-maker arrival proposal per auction minute; acceptance is validity-screened \\
 $p_3$ & 0.3 & Per-side, per-minute market-taker arrival proposal probability \\
 $p_4$ & 0.05 & Per-side, per-minute active-market-order cancellation proposal probability \\
-$\lambda$ & 2 & Inventory penalty \\
-$q$ & 1 & Shaping-treatment purchase-side coefficient; inactive in headline runs \\
-$k^\star$ & 1000 & CLOB shaping tolerance; inactive in headline runs \\
-$d$ & 0.1 & Cancellation-fee increment, $d_j=(j-n-1)d$ \\
-$\alpha$ & 0.01 & Tick size \\
-$\beta$ & 1 & Tick size of grid on $K^a$ \\
+$\lambda$ & 0.01 & Inventory penalty \\
+$q$ & 0 & No purchase subsidy; headline shaping is active \\
+$k^\star$ & 10000 & Clipping distance in ticks; local weight $S_0/(k^\star\alpha)=1$ \\
+$\omega_{\mathrm{sh}}$ & 0.0001 & Fictive-credit weight $\alpha/S_0$ \\
+$d$ & 0.001 & Stylized withdrawal-cost increment, $d_j=(j-n-1)d$ \\
+$\alpha$ & 0.01 & Tick in rebased model-price units \\
+$\beta$ & 0.25 & Slope step (inventory units per price unit) \\
 $K_\text{max}$ & 32 & Full slope-index lattice bound (zero included) \\
 $H_0$ & 100 & Initial projected clearing signal \\
 $\eta_H$ & 0.95 & Update weight in Algorithm \ref{alg:hyp_clearing_price}\\
