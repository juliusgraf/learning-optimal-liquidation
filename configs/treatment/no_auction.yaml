# Comparator terminating at auction open with residual inventory marked there.
experiment:
  ablation_label: H_off__shaping_off__auction_off
  auction_enabled: false
rl:
  h_cl_feature_enabled: false
actions:
  # Inactive in this comparator, but explicit so the resolved H-off treatment
  # remains mechanically coherent and can never expose H through action space.
  auction_anchor: frozen_mid
reward:
  shaping_enabled: false
  clob_shaping_enabled: false
  auction_shaping_enabled: false
  clawback_shaping: false
