# DRAFT alternative J_theta. Not the active manuscript J and not publication.
# theta=alpha/S0 moves fictive notional cash onto a one-tick execution scale.
# Development evidence remains insufficient for adoption; this is a model
# change requiring an explicit manuscript decision, not a headline override.
grid:
  alpha: 0.01
actions:
  beta: 1.0
auction_flow:
  U1: 1.0
  U2: 20.0
reward:
  k_star: 10000
  q: 0.0
  lambda_inv: 0.05
  auction_shaping_weight: 0.0001
rl:
  learning_credit_baseline: false
  learning_rate_half_life_episodes: 60.0
  learning_rate_min_fraction: 0.1
