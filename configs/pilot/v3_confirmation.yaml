# Apply only after the 100-episode stage-1 pilot passes the economic and simulator
# gates.  This is the longer multi-seed confirmation budget, not a pilot.

experiment:
  episodes: 800
  results_root: results/revision_v5

rl:
  validation_size: 100
  validation_frequency_episodes: 100
  validation_patience_evals: 6

algo:
  hyperparams:
    auction_learning_start_episode: 200
    epsilon_warmup_episodes: 50
    epsilon_decay_episodes: 600.0
