# pyproject.toml is the canonical dependency specification. Keeping this as
# an editable install of the project (including its test extras) prevents a
# second, incomplete dependency list from drifting out of sync.
-e .[dev]
